/**
 * @param {number[]} nums
 * @param {number} val
 * @return {number}
 */
var removeElement = function(nums, val) {
    let pointer = 0
    let swap = nums.length - 1

    while (pointer < swap) {
        if (nums[pointer] === val) {
            
        }
    }
};