class Solution {
    /**
     * @param {number[]} nums
     * @param {number} target
     * @return {number}
     */
    search(nums, target) {
        const left = 0
        const right = nums.length

        while(left < right) {
            break
        }

        return
    }
}
